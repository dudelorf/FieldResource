define(["marionette", "text!components/login/html/login.html"],
function(Marionette){
    
    var LoginView = Marionette.View.extend({
        
    });
    
    var views = {
        LoginView: LoginView
    };
});